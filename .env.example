# Sample values for BackupApp environment variables. Real values are injected
# by the Kubernetes CronJob via plain env and ExternalSecret-managed Secret.

# CouchDB connection (ClusterIP DNS, no Cloudflare Tunnel)
COUCHDB_URL=http://couchdb.databases.svc.cluster.local:5984
COUCHDB_USERNAME=obsidian-livesync-backup
COUCHDB_PASSWORD=replace-me
COUCHDB_DATABASE=obsidian-v2

# Self-hosted LiveSync E2E passphrase
LIVESYNC_PASSPHRASE=replace-me
# Path-obfuscation passphrase. Defaults to LIVESYNC_PASSPHRASE when unset.
# Set only when the vault uses a different value.
# LIVESYNC_OBFUSCATE_PASSPHRASE=
# Match the LiveSync client's "Use chunk splitter v2" setting.
LIVESYNC_CHUNK_SPLITTER_V2=true
# Match the LiveSync client's compression setting.
LIVESYNC_COMPRESSION=false
# Match the LiveSync client's case-sensitivity setting.
LIVESYNC_FILENAME_CASE_SENSITIVE=false

# Backup target git repository
GIT_REPOSITORY=https://github.com/example/example-vault.git
GIT_BRANCH=main
GIT_VAULT_SUBDIR=src

# octo-sts token exchange: trades the projected ServiceAccount token for a
# short-lived GitHub App installation token scoped to GIT_REPOSITORY.
OCTO_STS_URL=https://octo-sts.fohte.net
OCTO_STS_SCOPE=example/example-vault
OCTO_STS_IDENTITY=obsidian-livesync-backup
OCTO_STS_SA_TOKEN_PATH=/var/run/secrets/tokens/octo-sts-token

# Extra exclude rules (comma- or newline-separated)
EXCLUDE_PATHS=
# Regex patterns scanned against file contents; matches are masked.
EXCLUDE_SECRET_PATTERNS=
